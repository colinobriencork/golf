[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
ruff = ">=0.4.0"
mypy = ">=1.0.0"
pytest = ">=7.4.0"
pytest-mock = ">=3.11.1"
pytest-cov = ">=4.1.0"
pytest-timeout = ">=2.1.0"

[packages]
selenium = ">=4.11.2"
python-dotenv = ">=1.0.0"
webdriver-manager = ">=4.0.0"
pytz = "*"

[requires]
python_version = "3.13"
